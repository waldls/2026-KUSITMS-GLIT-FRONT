import { Suspense } from "react";

import StarLogSection from "@/containers/record/star-log/StarLogSection";

const Page = () => (
  <Suspense>
    <StarLogSection />
  </Suspense>
);

export default Page;
