const ExampleButton = ({ label }: { label: string }) => (
  <button className="rounded-full bg-yellow-500 px-5 py-3 text-sm font-bold text-black">
    {label}
  </button>
);

export default ExampleButton;
